import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.autenticadigital.madridagro',
  appName: 'Madrid Agro',
  webDir: 'dist'
};

export default config;
